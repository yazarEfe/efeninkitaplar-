import React from "react";
import ReactDOM from "react-dom/client";
import YeniAskerSite from "../yeni-asker-site";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <YeniAskerSite />
  </React.StrictMode>
);
